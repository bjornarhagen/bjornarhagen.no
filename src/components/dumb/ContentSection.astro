---
interface Props {
    className?: string;
}

const { className = "" } = Astro.props;
---

<section
    class={`${className} relative w-full max-w-screen-2xl mx-auto px-4 md:px-8`}
>
    <slot />
</section>
