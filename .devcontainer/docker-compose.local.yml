services:
    bjornarhagen-no:
        network_mode: host
        container_name: bjornarhagen-no
        build:
            context: .
            dockerfile: Dockerfile.local
        volumes:
            - ../:/app
        tty: true
